name: checks

on:
  push:
    branches:
      - "master"

jobs:
  flake_checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: cachix/install-nix-action@v20
      - run: nix flake check
